def smallest_positive(lst):
    smallest_pos = None
    for idx,num in enumerate(lst):
        if num > 0:
            if smallest_pos == None or num < smallest_pos:
                smallest_pos = num
                smallest_index = idx
    return smallest_index

print("Smallest Positive Number Is :",smallest_positive([30,-45,2,5]))
